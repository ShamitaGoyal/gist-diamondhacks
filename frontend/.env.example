# Gist Lens — optional frontend-only vars (`frontend/`).
# Do not put GEMINI_API_KEY here; the FastAPI backend reads it from the repo root `.env`.

# Only if the API is not proxied (default dev: Vite proxies `/api` → http://127.0.0.1:8000)
# VITE_API_BASE_URL=http://127.0.0.1:8000
