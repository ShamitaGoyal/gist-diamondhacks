# Copy this file to `.env` in the repo root (same folder as `run.py`) and fill in values.
# `.env` is gitignored — never commit real keys.

# Required for PDF Lens v2 and legacy `/analyze`: Google AI Studio API key
# https://aistudio.google.com/apikey
GEMINI_API_KEY=AIzaSyCAvkcEDB3_cokwGxIJcnqdBmswl_RgW80

# --- Legacy v1 (Node “PaperScope” API under v1/) ---
# Use `v1/.env.example` → copy to `v1/.env.local` for GEMINI_API_KEY, pacing, and port.
